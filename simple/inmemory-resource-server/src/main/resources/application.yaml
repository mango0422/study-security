spring:
  application:
    name: inmemory-resource-server
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000
logging:
  level:
    org.springframework.security: TRACE # Optional: for debugging
server:
  port: 8080